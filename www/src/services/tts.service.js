(function (global) { var NS = global.Phennellopy = global.Phennellopy || {}; NS.TTS = { speak: function (text) { return new Promise(function (resolve) { if (!global.speechSynthesis) return resolve(); var utterance = new SpeechSynthesisUtterance(String(text || '')); utterance.lang = 'pt-BR'; utterance.onend = resolve; utterance.onerror = resolve; global.speechSynthesis.cancel(); global.speechSynthesis.speak(utterance); }); } }; })(window);
